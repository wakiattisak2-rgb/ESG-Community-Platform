{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd aetros && npm install && npm run build",
  "outputDirectory": "aetros/dist",
  "installCommand": "cd aetros && npm install",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
