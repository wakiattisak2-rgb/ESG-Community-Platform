ESG Community Platform
<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ESG Community Platform — MVP Wireframe</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,300;9..144,400;9..144,500;9..144,700;9..144,900&family=IBM+Plex+Sans+Thai:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0f1410;
    --bg-card: #1a201b;
    --bg-elev: #232a24;
    --line: #2d3530;
    --ink: #e8ebe6;
    --ink-dim: #9aa39a;
    --ink-faint: #5d655e;
    --accent: #c4ff4d;
    --accent-dim: #8aab38;
    --warm: #e8c07d;
    --rust: #d97757;
    --note-bg: rgba(196, 255, 77, 0.08);
    --note-border: rgba(196, 255, 77, 0.3);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    font-family: 'IBM Plex Sans Thai', sans-serif;
    background: var(--bg);
    color: var(--ink);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
  }

  .display {
    font-family: 'Fraunces', serif;
    font-weight: 400;
    letter-spacing: -0.02em;
  }

  .mono {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    letter-spacing: 0.02em;
  }

  /* === Top intro === */
  .intro {
    max-width: 900px;
    margin: 0 auto;
    padding: 4rem 2rem 2rem;
  }

  .intro .tag {
    display: inline-block;
    color: var(--accent);
    border: 1px solid var(--accent-dim);
    padding: 0.3rem 0.8rem;
    border-radius: 999px;
    margin-bottom: 1.5rem;
  }

  .intro h1 {
    font-family: 'Fraunces', serif;
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    line-height: 1.05;
    margin-bottom: 1rem;
    font-weight: 400;
  }

  .intro h1 em {
    font-style: italic;
    color: var(--accent);
    font-weight: 300;
  }

  .intro p {
    color: var(--ink-dim);
    font-size: 1.05rem;
    max-width: 640px;
  }

  /* === Screen navigator === */
  .screen-nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(15, 20, 16, 0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--line);
    padding: 1rem 2rem;
    margin: 2rem 0;
  }

  .screen-nav-inner {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    gap: 0.5rem;
    overflow-x: auto;
    scrollbar-width: none;
  }
  .screen-nav-inner::-webkit-scrollbar { display: none; }

  .screen-nav button {
    background: transparent;
    border: 1px solid var(--line);
    color: var(--ink-dim);
    padding: 0.5rem 1rem;
    border-radius: 6px;
    cursor: pointer;
    font-family: inherit;
    font-size: 0.85rem;
    white-space: nowrap;
    transition: all 0.2s;
  }
  .screen-nav button:hover { color: var(--ink); border-color: var(--ink-faint); }
  .screen-nav button.active {
    background: var(--accent);
    color: var(--bg);
    border-color: var(--accent);
    font-weight: 500;
  }

  /* === Screen sections === */
  .screen {
    max-width: 1200px;
    margin: 0 auto 5rem;
    padding: 0 2rem;
    display: none;
  }
  .screen.active { display: block; }

  .screen-header {
    margin-bottom: 2rem;
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--line);
  }

  .screen-header .num {
    color: var(--accent);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    margin-right: 0.5rem;
  }

  .screen-header h2 {
    font-family: 'Fraunces', serif;
    font-weight: 400;
    font-size: 1.8rem;
  }

  .screen-header .why {
    color: var(--ink-dim);
    font-size: 0.9rem;
    max-width: 500px;
    text-align: right;
  }

  /* === Wireframe canvas === */
  .canvas {
    background: var(--bg-card);
    border: 1px solid var(--line);
    border-radius: 12px;
    overflow: hidden;
    margin-bottom: 1.5rem;
  }

  /* === Notes === */
  .notes {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1rem;
    margin-top: 1.5rem;
  }
  .note {
    background: var(--note-bg);
    border-left: 2px solid var(--accent);
    padding: 1rem 1.2rem;
    border-radius: 0 6px 6px 0;
  }
  .note .label {
    color: var(--accent);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    margin-bottom: 0.4rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }
  .note p { color: var(--ink); font-size: 0.88rem; line-height: 1.5; }

  /* ============================== */
  /* MOCK BROWSER CHROME            */
  /* ============================== */
  .browser-chrome {
    background: #0a0d0b;
    padding: 0.6rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.6rem;
    border-bottom: 1px solid var(--line);
  }
  .browser-chrome .dots { display: flex; gap: 6px; }
  .browser-chrome .dots span {
    width: 11px; height: 11px; border-radius: 50%;
    background: #2d3530;
  }
  .browser-chrome .url {
    flex: 1;
    background: var(--bg);
    color: var(--ink-dim);
    padding: 0.3rem 0.8rem;
    border-radius: 4px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
  }
  .browser-chrome .url .green-dot {
    display: inline-block;
    width: 6px; height: 6px;
    background: var(--accent);
    border-radius: 50%;
    margin-right: 6px;
    vertical-align: middle;
  }

  /* ============================== */
  /* APP HEADER (in mocks)          */
  /* ============================== */
  .app-header {
    background: var(--bg-elev);
    padding: 1rem 1.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid var(--line);
  }
  .app-logo {
    font-family: 'Fraunces', serif;
    font-size: 1.1rem;
    font-weight: 500;
  }
  .app-logo span { color: var(--accent); }
  .app-nav { display: flex; gap: 1.5rem; }
  .app-nav a {
    color: var(--ink-dim);
    text-decoration: none;
    font-size: 0.85rem;
  }
  .app-nav a.active { color: var(--ink); }
  .app-cta {
    background: var(--accent);
    color: var(--bg);
    padding: 0.45rem 1rem;
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 500;
  }

  /* ============================== */
  /* SCREEN 1: LANDING              */
  /* ============================== */
  .hero {
    padding: 3.5rem 1.5rem 2.5rem;
    background: linear-gradient(135deg, var(--bg-card) 0%, var(--bg-elev) 100%);
    position: relative;
    overflow: hidden;
  }
  .hero::before {
    content: '';
    position: absolute;
    top: -50%; right: -10%;
    width: 400px; height: 400px;
    background: radial-gradient(circle, rgba(196,255,77,0.08) 0%, transparent 70%);
    pointer-events: none;
  }
  .hero-eyebrow {
    color: var(--accent);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 1rem;
  }
  .hero h1 {
    font-family: 'Fraunces', serif;
    font-size: 2.6rem;
    line-height: 1.05;
    font-weight: 400;
    max-width: 700px;
    margin-bottom: 1rem;
  }
  .hero h1 em { font-style: italic; color: var(--accent); font-weight: 300; }
  .hero p {
    color: var(--ink-dim);
    max-width: 540px;
    margin-bottom: 2rem;
  }
  .hero-ctas { display: flex; gap: 0.8rem; flex-wrap: wrap; }
  .btn-primary {
    background: var(--accent);
    color: var(--bg);
    padding: 0.7rem 1.4rem;
    border-radius: 6px;
    font-weight: 500;
    border: none;
    cursor: pointer;
  }
  .btn-ghost {
    background: transparent;
    color: var(--ink);
    border: 1px solid var(--line);
    padding: 0.7rem 1.4rem;
    border-radius: 6px;
    cursor: pointer;
  }
  .hero-stats {
    display: flex;
    gap: 2.5rem;
    margin-top: 2.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--line);
  }
  .stat .num {
    font-family: 'Fraunces', serif;
    font-size: 1.6rem;
    color: var(--ink);
  }
  .stat .label {
    font-size: 0.75rem;
    color: var(--ink-faint);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  /* Content sections (landing) */
  .section-block {
    padding: 2.5rem 1.5rem;
    border-bottom: 1px solid var(--line);
  }
  .section-title {
    font-family: 'Fraunces', serif;
    font-size: 1.4rem;
    margin-bottom: 0.4rem;
  }
  .section-sub {
    color: var(--ink-dim);
    font-size: 0.9rem;
    margin-bottom: 1.5rem;
  }

  .article-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1rem;
  }
  .article-card {
    background: var(--bg);
    border: 1px solid var(--line);
    border-radius: 8px;
    padding: 1.2rem;
  }
  .article-card .topic {
    display: inline-block;
    color: var(--accent);
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    margin-bottom: 0.6rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }
  .article-card h4 {
    font-family: 'Fraunces', serif;
    font-weight: 500;
    margin-bottom: 0.4rem;
    font-size: 1rem;
    line-height: 1.3;
  }
  .article-card .meta {
    color: var(--ink-faint);
    font-size: 0.75rem;
    margin-top: 0.8rem;
    display: flex;
    gap: 0.8rem;
  }

  /* ============================== */
  /* SCREEN 2: KNOWLEDGE HUB        */
  /* ============================== */
  .hub-layout {
    display: grid;
    grid-template-columns: 220px 1fr;
    min-height: 500px;
  }
  .hub-sidebar {
    background: var(--bg);
    padding: 1.5rem 1rem;
    border-right: 1px solid var(--line);
  }
  .hub-sidebar h5 {
    font-size: 0.7rem;
    color: var(--ink-faint);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    margin-bottom: 0.8rem;
    margin-top: 1.4rem;
  }
  .hub-sidebar h5:first-child { margin-top: 0; }
  .hub-sidebar a {
    display: block;
    color: var(--ink-dim);
    text-decoration: none;
    font-size: 0.85rem;
    padding: 0.4rem 0.6rem;
    border-radius: 4px;